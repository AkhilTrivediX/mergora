{
  "schemaVersion": 1,
  "itemId": "meter",
  "entryExport": "Meter",
  "refTarget": "HTMLMeterElement",
  "exports": [
    { "kind": "component", "name": "Meter" },
    { "kind": "type", "name": "MeterProps" }
  ],
  "controlledState": ["value"],
  "nativePolicy": [
    "A visible label wraps one native meter with finite ordered minimum, maximum, low, high, optimum, and value constraints.",
    "Visible and aria-valuetext formatting share one localized string.",
    "Role, generic accessible-name props, and aria value overrides are rejected at type and runtime boundaries because the visible label and native meter model own them.",
    "Meter remains visually and semantically distinct from task progress."
  ]
}
