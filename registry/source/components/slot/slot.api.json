{
  "schemaVersion": 1,
  "itemId": "slot",
  "entryExport": "Slot",
  "refTarget": "HTMLElement",
  "nativeProps": "Omit<React.HTMLAttributes<HTMLElement>, children>",
  "exports": [
    { "kind": "component", "name": "Slot" },
    { "kind": "function", "name": "isSlottableChild" },
    { "kind": "type", "name": "SlotProps" }
  ],
  "props": [{ "name": "children", "required": true, "type": "one concrete ReactElement" }],
  "compositionPolicy": {
    "precedence": "Child semantics, accessible-name attributes, style values, and native props are authoritative.",
    "events": "Child handlers run first; slot handlers run unless the child prevents default.",
    "refs": "Forwarded and child refs receive the same concrete DOM node.",
    "invalidChildren": "Fragments and non-elements fail with a precise error."
  }
}
