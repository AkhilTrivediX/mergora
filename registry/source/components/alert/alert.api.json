{
  "schemaVersion": 1,
  "itemId": "alert",
  "entryExport": "Alert",
  "refTarget": "HTMLDivElement",
  "exports": [
    { "kind": "component", "name": "Alert" },
    { "kind": "type", "name": "AlertProps" },
    { "kind": "type", "name": "AlertVariant" },
    { "kind": "type", "name": "AlertLiveMode" },
    { "kind": "type", "name": "AlertHeadingLevel" }
  ],
  "controlledState": [],
  "nativePolicy": [
    "Static alerts make no announcement; explicit polite/assertive modes require a concise announcement and use the shared announcer.",
    "A caller-selected native heading, visible variant label, text, shape, and optional actions prevent color-only severity.",
    "Provider labels remain stable while explicit non-empty variantLabel wins; actions never enter the live region."
  ]
}
