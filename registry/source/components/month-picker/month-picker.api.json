{
  "schemaVersion": 1,
  "itemId": "month-picker",
  "entryExport": "MonthPicker",
  "refTarget": "HTMLInputElement",
  "nativeProps": "React input attributes except owned type, value, defaultValue, and change fields",
  "exports": [
    { "kind": "component", "name": "MonthPicker" },
    { "kind": "type", "name": "MonthPickerProps" }
  ],
  "controlledState": [
    {
      "state": "month",
      "controlled": "value",
      "uncontrolled": "defaultValue",
      "callback": "onValueChange"
    }
  ],
  "props": [
    { "name": "showQuarterContext", "required": false, "default": false, "type": "boolean" },
    {
      "name": "quarterContext",
      "required": false,
      "type": "ReactNode | (quarter, year) => ReactNode"
    }
  ],
  "compositionPolicy": {
    "native": "The native month control owns localized editing, year navigation, validation, form serialization, and reset.",
    "quarter": "Quarter context is derived and associated only when showQuarterContext is true and the month is complete."
  }
}
