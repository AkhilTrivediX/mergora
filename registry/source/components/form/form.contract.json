{
  "schemaVersion": 1,
  "contractVersion": "0.1.0",
  "itemId": "form",
  "contractStatus": "source-present-unreleased",
  "claim": "No Stable, release, conformance, package-parity, or recorded manual assistive-technology claim is made by this source contract.",
  "riskClass": 1,
  "semantics": {
    "nativeBasis": "native form",
    "naming": "The form landmark is named only when more than one form landmark requires disambiguation.",
    "roleOverride": "Consumer role overrides are outside the supported contract.",
    "refTarget": "native root"
  },
  "interaction": {
    "keyboard": "Native controls and submit buttons own Enter, Space, Tab, and Shift+Tab behavior.",
    "pointer": "Native pointer activation and cancellation remain authoritative for interactive descendants.",
    "touch": "Coarse-pointer presentation preserves at least a 44 CSS pixel preferred target where the item is interactive.",
    "speech": "Visible labels remain available for voice targeting.",
    "switchAccess": "No duplicate proxy focus target is introduced."
  },
  "formBehavior": "No submit or reset event is intercepted; native URL actions and React server actions pass through. Non-empty optional submission status is consumer-owned output and derives aria-busy only while submitting; false, null, omitted, and statically empty messages emit no status semantics or derived state.",
  "states": {
    "attributes": ["data-layout", "data-submission-state", "aria-busy"],
    "synchronization": "Visible, programmatic, and native form states are kept synchronized without hidden behavior engines. Disabled or statically empty submission status emits no visual, event, data-state, role, live-region, or derived busy output."
  },
  "stableSelectors": {
    "root": "[data-slot=form]",
    "attributes": ["data-layout", "data-submission-state", "aria-busy"]
  },
  "preferences": {
    "dark": "Uses semantic theme tokens.",
    "enhancedContrast": "Uses semantic theme tokens and non-color state cues.",
    "forcedColors": "Uses system colors, visible borders, and system-color focus indicators.",
    "reducedMotion": "No essential information depends on motion; Switch removes its thumb transition."
  },
  "responsive": {
    "minimumInlineSizeCssPx": 320,
    "supports400PercentZoom": true,
    "supports200PercentText": true,
    "logicalProperties": true,
    "overflowPolicy": "Labels and messages wrap without clipping; form values retain native scrolling where applicable."
  },
  "internationalization": {
    "consumerStrings": ["submissionStatus"],
    "directions": ["ltr", "rtl"],
    "locales": ["en-US", "de-DE", "ar-EG", "he-IL", "ja-JP", "hi-IN", "en-XA", "ar-XB"],
    "imePolicy": "Native edit controls retain browser composition behavior; non-edit controls do not intercept composition.",
    "serialization": "Serialized names and values remain consumer-owned machine data and are never localized by the component."
  },
  "consumerResponsibilities": [
    "Provide an explicit submit button for non-trivial forms.",
    "Use native names and values for every successful control.",
    "Keep adapter validation messages and pending status localizable."
  ],
  "evidenceRequirements": {
    "automated": [
      "strict-types",
      "ssr-server-boundary",
      "native-submit-reset",
      "server-action",
      "adapter-fixtures",
      "successful-controls",
      "submission-status-disabled-absence",
      "packed-consumer",
      "package-source-parity"
    ],
    "manual": ["keyboard-submit-reset", "screen-reader-form", "mobile-authentication"],
    "recordedEvidence": []
  },
  "limitations": [
    "Source-present-unreleased records do not include immutable generated artifacts, packed-consumer evidence, package/source parity, or current manual assistive-technology sessions."
  ]
}
